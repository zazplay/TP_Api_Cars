/// <reference types="react" />
export declare type PaginationControlProps = {
    page?: number;
    between?: number;
    total: number;
    limit: number;
    changePage?: (page: number) => any;
    next?: boolean;
    last?: boolean;
    ellipsis?: number;
};
export declare const PaginationControl: ({ page, between, total, limit, changePage, next, last, ellipsis }: PaginationControlProps) => JSX.Element;
